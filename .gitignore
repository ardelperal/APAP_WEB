# Access binaries and lock files
*.accdb
*.laccdb
*.mdb
*.ldb

# Dysflow local state
.dysflow/project.json
.dysflow/runtime/

# Legacy/local Access sync session metadata
.access-vba-skill/

# Generated skill-registry cache
.atl/.skill-registry.cache.json

# Agent receipt files dropped into .atl/ by tooling — local noise, never
# source. Narrow rule (issue #207): only *.md inside .atl/, with the one
# deliberately tracked registry kept via negation.
.atl/*.md
!.atl/skill-registry.md

# Local/generated data and binary asset folders
Anexos/
Fotos/
Informes/
Seguimientos/
contratos/
recursos/
www/
Plantillas/
iconos/
conf/

# OS/editor noise
*.tmp
*.bak
*.log
Thumbs.db
.DS_Store

# Local tool caches (not source)
.engram/
.playwright-mcp/
.codegraph-vba/

# Python build and test artifacts
# Added by openspec/changes/ci-cd-foundation (task 0.5, PR 1).
__pycache__/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.coverage

# Local-only operator secrets and local coverage snapshots.
# W1 secret-leak remediation (PR4a follow-up). Narrow scope: project root
# only; no recursive or double-star patterns. The ``.codegraph`` index and
# ``.env.example`` template (if added) remain tracked.
/.env
/coverage.json
/coverage_full.json

# Virtual environment
.venv/
venv/
env/

# Tailwind v4 build artifacts
tailwindcss/node_modules/
tailwindcss/package-lock.json

# Tool config with secrets in plaintext (InsForge API key)
# DO NOT commit. Use environment variables instead.
opencode.json

# External artifacts accidentally placed in the project root by other systems.
# Not project source — keep them local.
.openclaw/
SOUL.md
.audit-venv/
