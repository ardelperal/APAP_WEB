"""Positive fixture: integration test that covers build_foo.

The detector expects this file to exist at the fixture root.
This is a stub — the detector only checks for file existence, not runtime behavior.
"""

from __future__ import annotations


def test_build_foo() -> None:
    # Minimal stub — the detector only checks for existence of this file.
    pass
