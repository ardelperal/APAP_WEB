"""Test fixtures for the AST-based rule linter (scripts/check_rules.py).

Each detector has a positive fixture (must trigger) and a negative fixture
(must NOT trigger) so the parametrized suite can prove both directions.
"""
