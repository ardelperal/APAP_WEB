# gitleaks allowlist — issue #381
#
# One entry per confirmed false positive, each with the date it was assessed
# and why it is not a secret. Never silence a finding by weakening the ruleset
# or narrowing the scan path; add it here with a reason instead.
#
# Format: <fingerprint>  (see `gitleaks dir --report-format json` for the value)

# 2026-08-01 — rule `generic-api-key` in
# tests/migration/test_insforge_storage_methods.py
# The flagged value is a storage object FILENAME used as a test fixture, not a
# credential. The adjacent SERVICE_KEY on the line above is likewise a fixture
# string. Confirmed by reading the file; nothing here reaches a real InsForge
# tenant.
#
# Do NOT quote the flagged values in this file. The first version of this
# comment did, and gitleaks flagged .gitleaksignore itself — an allowlist that
# creates the finding it allowlists.
tests/migration/test_insforge_storage_methods.py:generic-api-key:40
