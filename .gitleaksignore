# gitleaks allowlist — issue #381
#
# One entry per confirmed false positive, each with the date it was assessed
# and why it is not a secret. Never silence a finding by weakening the ruleset
# or narrowing the scan path; add it here with a reason instead.
#
# Format: <fingerprint>  (see `gitleaks dir --report-format json` for the value)

# (allowlist entries for the legacy migration storage tests were retired
# in the test-fakes cleanup slice; the test files are gone and this
# section is now dead config kept for the historical record of issue #381.)

# 2026-09-08 — issue #681 jscpd-ratchet follow-up. All three are hardcoded
# test-only fixture values, never live secrets:
#   - tests/integration/test_magic_link_routes.py:79 — a padding string
#     used only to satisfy `_validate_secrets`'s 32-char minimum for
#     `APAP_SESSION_SECRET` in an integration-test lifespan.
#   - tests/test_maildev_helper.py:77 and :83 — fake hex/urlsafe tokens
#     used to test `VERIFY_URL_RE` regex matching, not real magic-link
#     tokens issued by `secrets.token_hex`/`secrets.token_urlsafe`.
tests/integration/test_magic_link_routes.py:generic-api-key:79
tests/test_maildev_helper.py:generic-api-key:77
tests/test_maildev_helper.py:generic-api-key:83
