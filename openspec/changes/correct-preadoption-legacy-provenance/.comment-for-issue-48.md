<!-- cancellation-marker:adopt-02 -->
Issue #48 cancelled for invalid legacy provenance.

The 20-day decision clause belongs to foster care (`APAP_ACTUAL/src/classes/Plantilla.cls`, `RellenarContratoAcogida`, lines 381-391), NOT to pre-adoption lifecycle. Pre-adoption remains active until definitive adoption or explicit return, governed by `FDevolucion IS NULL` (`APAP_ACTUAL/src/classes/Adopcion.cls`, `AnimalConAdopcionesActivas`, lines 1991-2054). No automatic expiry, no timer, no worker, no notification.

The runtime registry (`APAP_ACTUAL/src/classes/Entorno.cls` L793) selects the same `CONTRATO DE ADOPCIÓN_V02.docx` for both Adopción and PreAdopción; `RellenarContratoPreAdopcion` (`Plantilla.cls` L620-689) contains no one-month or other automatic timer.

Cancelled by change `correct-preadoption-legacy-provenance` on branch `docs/correct-preadoption-legacy-provenance`. ADOPT-01 invariants preserved unchanged.

Forensic preservation:
- `feat/adopt-02-expiry` branch kept at `40ce83f` (not deleted)
- `stash@{0}` `adopt-02-invalid-work-2026-07-10` kept (not popped)
- 3 invalid local-main commits (`2ea1765`, `be43c14`, `40ce83f`) kept ahead of `origin/main`
- ADOPT-01 test locks in `tests/test_adopciones.py`, `tests/test_adopciones_routes.py`, `tests/test_domain.py::test_adopciones_create_table_sql_columns` untouched

If a real expiry policy is ever needed, it must be a separately approved business change.
