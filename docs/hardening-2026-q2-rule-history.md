# Hardening 2026-Q2 — Resolved rule conflicts (history)

This is the archived "Known conflicts with existing code" tracker that used to
live in `AGENTS.md`. Every row here is **DONE** — the live rule it relates to
now stands on its own in `AGENTS.md` with its enforcement mechanism. This file
is kept only as the audit trail of how each pre-existing violation was closed
during the `hardening-2026-q2` chain.

It is **not** loaded as agent instructions. For the active rules, see
`AGENTS.md`. For the per-slice audits, see `docs/audits/`.

| Rule | Where | Reason it still existed | Follow-up (closed) |
|---|---|---|---|
| 4 — one source of truth | ~~`app/core/auth.py:30` (`VALID_ROLES` hardcoded) and `app/modules/voluntarios/service.py:47` (`VALID_ROL_TYPES` hardcoded)~~ | ~~The `StrEnum` for roles did not exist yet. The frozenset was the only source.~~ | **DONE** — Slice 2 (PR-2). `VALID_ROLES` and `VALID_ROL_TYPES` are now derived from their `StrEnum`s; the inline `CHECK (rol IN (...))` in `CREATE_TABLE_SQL` was removed; migration `004_drop_rol_check.sql` cleans up the constraint on databases deployed before PR-2. See `openspec/changes/hardening-2026-q2/specs/02-rule-4-ddl/spec.md` and audit `engram:14516`. |
| 6 — defaults deny | ~~`app/core/auth_dependencies.py:80` used `payload.get("is_authorized", True)` (default permits)~~ — **FIXED at PR-3 of hardening-2026-q2** | Was deliberate: pre-fix cookies didn't carry the flag. The default `True` kept them working. | **DONE** — Slice 3 PR-3 flipped both call sites (`app/main.py:165` middleware + `app/core/auth_dependencies.py:130` dependency) to `payload.get("is_authorized", False)`. For pre-fix cookies still in flight, rotate `APAP_SESSION_SECRET` per [`docs/runbooks/cookie-rotation.md`](runbooks/cookie-rotation.md). |
| 7 — no `HTTPException` for redirects | ~~`app/core/auth_dependencies.py:77-83` raised `HTTPException(302, ...)`~~ — **FIXED at PR-5A of hardening-2026-q2** | The original `AGENTS.md` row was outdated: the function already used `RedirectResponse`. The Q2 audit verified both redirect branches return `RedirectResponse(url=..., status_code=302)` at `app/core/auth_dependencies.py:138-141`. No `HTTPException(status_code=302` exists in `app/`. | **DONE** — Slice 5 PR-5A. Audit at [`docs/audits/auth-dependencies-audit-2026-Q2.md`](audits/auth-dependencies-audit-2026-Q2.md); static source regression test at `tests/test_rule_7_compliance.py::test_uses_redirectresponse_not_http_exception_in_require_authorized_user`. |
| CSRF defense-in-depth | `Settings.csrf_enabled: bool = True` controls `CsrfMiddleware` (`app/core/csrf.py`). Default True; set `APAP_CSRF_ENABLED=false` for emergency rollback only. | PR-5B introduced the gate on Slice 5's audit HIGH finding (F-1, missing CSRF defense on 10 POST handlers). The middleware validates `X-CSRFToken` header AND `csrf_token` form field against the session-bound token; both `apap_session` and `apap_pkce` cookies ship with `SameSite=Strict`. | **DONE** — Slice 5 PR-5B1 + PR-5B2. All 10 POST handlers render the hidden CSRF input; `tests/test_csrf_middleware.py` (9 cases), `tests/test_all_post_forms_have_csrf_input.py` (10 cases), `tests/test_csrf_form_enumeration.py` (10 cases). Slice 6 swapped the `csrf.rejected` / `csrf.disabled` logging placeholder for `log_safe()` in PR-6A.2; `tests/test_csrf_rejected_log_event.py` pins the event-name contract. |
| 9 — `log_safe()` only | New rule (PR-6A + PR-6B, Slice 6). Banned direct `logging.getLogger(__name__).{info,warning,error,debug,critical,exception}(...)` in `app/` via APAP003 (Detector 5 in `scripts/check_rules.py` + `APAP003Visitor` in `scripts/ruff_plugin/apap_rules.py`). Redaction list has 12 closed fields (round-2 fix SB-5). `app/core/logging.py` is the only excluded path. | The Q2 audit (`engram:14518`) found zero `logger.*` calls in `app/`, so operators had no observability. | **DONE** — Slice 6 PR-6A.1 + PR-6A.2 + PR-6B. `app/core/logging.py` ships `JsonFormatter`, `RedactionFilter`, `configure_logging(settings)`, `log_safe(event, **fields)`; `app/main.py` lifespan calls `configure_logging(settings)` first; `tests/test_logging.py` (32 cases) + `tests/test_logging_redaction_adversarial.py` (42 cases) + `tests/test_apap003.py` (14 cases). Known limitation (round-2 fix SB-7): APAP003 bans the chained call but not the bare `logging.getLogger(__name__)` retrieval. |
